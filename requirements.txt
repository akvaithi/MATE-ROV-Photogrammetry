PyQt6==6.8.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==11.1.0
loguru==0.7.3

# Reconstruction uses Apple's RealityKit / Object Capture (macOS only) via a
# compiled Swift helper — no Python packages required, just Xcode Command Line
# Tools (`xcode-select --install`).
#
# The in-app 3D preview renders the USDZ with macOS Quick Look (`qlmanage`), so
# no Python 3D library (pyvista/VTK) is needed.
